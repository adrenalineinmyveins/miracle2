1.fork()被调用时，有3种不同的返回值
	在父进程中，fork()返回新建的子进程PID;
	在子进程中，返回0;	
	出现错误，返回一个负值。
2.fork() 的子 父进程同时相互独立地运行，没有先后顺序

3.vfork()  的子父进程存在相关运行顺序
4.进程创建函数 fork()中，父进程的数据空间/堆栈空间都会给子进程一个拷贝，而不是共享这些内存。
	而在vfork()中，子进程与父进程共享数据段。
5.fork()不对子/父进程的执行次序有任何限制；vfork()中子进程先运行，父进程挂起。
